program MaxDemo;
var
	a : byte = 17;
	b : byte = 5;
	result : byte;

begin
	clearscreen(key_space,screen_char_loc);
	result := max(a,b);
	moveto(0,0,hi(screen_char_loc));
	printdecimal(result,3);

	loop();
end.
