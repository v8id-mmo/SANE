program CloseIRQDemo;
var
	frame : byte = 0;

interrupt Raster();
begin
	startirq(0);
	inc(frame);
	screen_bg_col := frame;
	closeirq();
end;

begin
	disableciainterrupts();
	startrasterchain(Raster(), 100, 0);
	loop();
end.
