# Constants

This page is a placeholder. Predefined constants (124 total, scoped to the
C64 target) are documented as grouped tables here rather than one page
each. See `DOCS_PROGRESS.md`'s "Constants" section for the planned
grouping (Colors, Boolean/null, Keyboard, Joystick, Screen/VIC registers,
Sprite registers, Sound/SID, 6502 registers). Content is planned for a
later batch.
