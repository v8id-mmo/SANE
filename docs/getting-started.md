# Getting Started

This page is a placeholder. Full content is planned for a later batch.
For now:

- SANE compiles from the command line only (`-cli` mode); there is no
  bundled GUI/IDE.
- The compiler targets the Commodore 64 exclusively.
- See the [Reference](reference/types.md) section for the language itself,
  and [Examples](examples/index.md) for runnable programs.
