program TestMain;
var
	i : byte = 0;

begin
	screen_bg_col := blue;
	screen_fg_col := LIGHT_BLUE;

	moveto(1, 1, hi(screen_char_loc));
	printstring("HELLO C64 FROM TRSE CLI", 0, 40);

	for i := 0 to 10 do
	begin
	end;

	loop();
end.
