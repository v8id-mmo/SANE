program Bug_2_15_IgnoreSystemHeadersNoEffect;
// Baseline twin for 2_15_ignoresystemheaders_no_effect.ras's .diffcheck:
// byte-identical except for the missing @ignoresystemheaders line, same
// internal program name (so the emitted label text matches too). Not a
// bug snippet on its own; excluded from run_regressions.sh's default
// sweep (any *_baseline.ras is skipped when no explicit names are given).
var
	x : byte;
begin
	x := 1;
	screen_bg_col := x;
	loop();
end.
